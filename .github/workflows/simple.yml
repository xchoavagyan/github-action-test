name: Shell Commands
on: [push]
jobs:
  run-shell-command: 
    runs-on: ubuntu-latest
    steps:
      - name: echo String
        run: eccho "String for echo"
      - name: multiline
        run: |
          node -v
          npm -v
